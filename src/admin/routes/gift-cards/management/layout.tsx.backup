import { Outlet } from "react-router-dom"
import { defineRouteConfig } from "@medusajs/admin-sdk"

const GiftCardsManagementLayout = () => {
  return <Outlet />
}

export const config = defineRouteConfig({
  label: "Management",
})

export default GiftCardsManagementLayout
