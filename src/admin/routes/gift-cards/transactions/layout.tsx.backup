import { Outlet } from "react-router-dom"
import { defineRouteConfig } from "@medusajs/admin-sdk"

const GiftCardsTransactionsLayout = () => {
  return <Outlet />
}

export const config = defineRouteConfig({
  label: "Transactions",
})

export default GiftCardsTransactionsLayout
